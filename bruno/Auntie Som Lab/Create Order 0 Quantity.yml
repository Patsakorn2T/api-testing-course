info:
  name: Create Order 0 Quantity
  type: http
  seq: 9

http:
  method: POST
  url: "{{baseUrl}}/orders"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "itemId": 1,
        "quantity": 0
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        test("zero quantity should be rejected", function () {
          expect(res.getStatus()).to.be.oneOf([400, 422]);
        });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
